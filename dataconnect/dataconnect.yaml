specVersion: "v1beta"
serviceId: "Login"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "Login-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
